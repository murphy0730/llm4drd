# -*- coding: utf-8 -*-
"""第5章 ALNS 自适应大邻域搜索：先大刀阔斧拆，再聪明地补。"""

BLOCKS = [
    ('h1', '第5章 ALNS 自适应大邻域搜索：先大刀阔斧拆，再聪明地补'),
    ('p', '本章导读：前面章节讲的派工规则是"一条规则用到底"，NSGA-III 负责"广撒网找一堆互不占优的好方案"，而本章的 ALNS 是项目里的"精修师傅"——它拿到一个还不错的方案后，反复做两件事：把方案拆掉一大块，再聪明地补回去，越补越好。学完本章，你会明白大邻域搜索为什么比小修小补更容易跳出死胡同、"自适应"三个字在代码里到底是怎么体现的，以及本项目为什么要在经典 ALNS 外面再套一层 NSGA-III。'),

    # ---------------------------------------------------------- 5.1
    ('h2', '5.1 从"每次挪一道工序"到"整格倒出来重摆"：大邻域搜索的思想'),
    ('p', '先建立两个行话。**局部搜索**是一类最朴素的改进方法：手上有一个方案，每次只对它做一点点改动（比如把某道工序换到另一台机器），改完更好就收下，更好不了就停。**邻域**就是"从当前方案出发，一步能够到的所有方案"的集合——你规定的改动动作越小，邻域就越小。'),
    ('p', '小邻域有个经典毛病：容易卡在**局部最优**。所谓局部最优，就是"附近一圈都找不出更好的，但放眼全局其实并不是最好"的方案。用迷你案例来说：FIFO 规则跑出的排程总延误是 6 小时（订单 A 延 2 小时、订单 B 延 4 小时），如果你只允许"每次挪一道工序"，很可能怎么挪都有一张订单来不及，挪来挪去总延误还是降不到 0——而精确求解器证明，这个案子总延误 0 是做得到的（CP-SAT 以总延误为目标求出了 OPTIMAL 的 0）。中间隔着一道"坎"，小碎步迈不过去。'),
    ('quote', '**大邻域搜索（Large Neighborhood Search, LNS）**：每次迭代不是小改一下，而是先把当前方案"拆掉一大批"（比如拆掉 18% 的工序），再用一套修复规则"重新塞回去"。因为一步跨得远，邻域非常大，所以有机会直接跳过把小邻域困住的那道坎。'),
    ('p', '生活类比：整理衣柜。普通局部搜索是"每次只换一件衣服的位置"，柜子整体格局基本不动，换来换去差不多；LNS 是"把一格抽屉整个倒出来，按新思路重新摆一遍"——动作大，但才可能真正换一种格局。当然，倒出来容易、摆回去难，所以 LNS 的成败全在"怎么拆、怎么补"，这正是 5.2 和 5.5 的主题。'),
    ('p', 'ALNS 就是在 LNS 前面加了一个字母 A（Adaptive，自适应）：拆和补各准备了好几种算子（策略），系统边用边学，哪种算子最近老立功就多派它上场。这个 A 留到 5.3 细讲。'),

    # ---------------------------------------------------------- 5.2
    ('h2', '5.2 破坏与修复：本项目拆的是"参数"，不是"排程"'),
    ('p', '教科书里的 LNS/ALNS 大多用在车辆路径、排程这类问题上，标准做法是：破坏算子直接从排程表里删掉一批工序，修复算子再按"插入成本"把它们逐个插回时刻表（著名的 regret 插入就是"先插最亏不起的那道工序"）。**本项目实现差异**：本项目的破坏和修复根本不碰排程表，它们作用在"参数空间"上。'),
    ('p', '回忆一下本项目解的编码：一个**候选个体**（`CandidateParameters`，见 optimization/solution_model.py）不是一张排程表，而是"一套派工规则的参数"——16 个特征权重、外加一个稀疏的 **op_bias** 字典。op_bias 是"给某道工序的额外加分"：仿真器每次派工时，对所有候选工序按规则打分，分数最高者上机；如果某道工序带 +0.4 的 op_bias，它就更容易被顶到前面。真正的排程，是仿真器拿着这套参数重跑一遍派工规则之后才诞生的。'),
    ('p', '于是破坏和修复在代码里长这样（见 optimization/hybrid_nsga3_alns.py:_destroy_ops 与 _repair_ops）：'),
    ('bullets', [
        '**破坏（destroy）**：按 destroy_fraction（默认 0.18，夹在 0.05~0.5）挑出一批工序，把它们的 op_bias 条目从字典里删掉（`candidate.op_bias.pop(op_id)`）——相当于"收回对这批工序的特殊照顾"。',
        '**修复（repair）**：给这批被拆的工序按某种规则重新写上 op_bias 加分，同时把 16 个特征权重做一点微调——相当于"换一套思路重新照顾它们"。',
        '**重生成排程**：参数改完后，评估函数用完整离散事件仿真重跑一遍，得到新排程和各项目标值。所谓"把工序塞回排程"，是仿真器隐式完成的，代码里不存在"插入成本"这个概念。',
    ]),
    ('p', '整个精炼主循环（见 optimization/alns_core.py:ALNSCore.refine）可以概括成下面这张伪代码：'),
    ('code', '每次迭代（每个精英约 6 次）：\n  1. 轮盘赌选出 1 个破坏算子 + 1 个修复算子\n  2. 复制当前个体，取上一解的分析数据（图特征、瓶颈等）\n  3. 破坏：删除被选中工序的 op_bias\n  4. 修复：按规则给这些工序重写 op_bias、微调特征权重\n  5. 仿真器重跑派工规则，得到新排程与目标值\n  6. 接受判定：支配则必换，否则模拟退火掷骰子\n  7. 按结果奖惩两个算子的权重；温度 x 0.92'),
    ('p', '用迷你案例走一遍完整流程。假设当前个体跑出来的正好是 FIFO 那张排程：makespan=13，总延误=6（A 延 2、B 延 4），于是分析数据里超期订单是 O1、O2。破坏算子把 O1、O2 的工序收进候选池 {A1, A2, A3, B1, B2}，按 destroy_fraction=0.18 抽样：count = ceil(5 x 0.18) = ceil(0.9) = 1，即随机拆 1 道（见 choose_subset，hybrid_nsga3_alns.py:1193）。假设拆中了 B1，它原来的 op_bias（比如 +0.20）被删掉。接着修复算子（比如 due_date_repair）按任务交期升序给被拆工序重新加分：B1 所属任务 T2 的交期是 8，排第 0 名，得 +0.40 - 0.05 x 0 = +0.40，同时全局特征权重 due_date +0.18、urgency +0.12、slack +0.08。'),
    ('p', '参数改完，仿真器重跑：B1 带着 +0.40 的加分，t=0 时得分被显著抬高，很可能抢到 M1 先开工；A 链工序也因为 urgency 权重上升而更受重视。这样排出来的新排程会向 EDD 那张的样子靠拢——而 EDD 排程实测总延误=0、makespan=14。也就是说，"删一个加分、重写一个加分"这种看似间接的操作，通过仿真器这座桥，真实地换来了一个更好的排程。这就是"参数空间邻域"的全部含义：ALNS 动的是基因，仿真器负责把基因翻译回排程。'),

    # ---------------------------------------------------------- 5.3
    ('h2', '5.3 自适应的"A"：哪个算子好用，下次就多派它上场'),
    ('quote', '**自适应（Adaptive）**：不事先规定每个算子用多少次，而是边跑边记账——每个算子都有一个权重，立功就加码、吃瘪就减码；选算子时按权重抽 Lottery，权重越大被抽中的概率越高。'),
    ('p', '本项目的奖惩规则是"乘性更新"，写在 optimization/alns_core.py:ALNSCore._update_operator 里，每次迭代结束立刻结算：'),
    ('formula', '改进： w <- w x 1.12 + 0.08 ｜ 仅接受： w <- w x 1.04 + 0.03 ｜ 被拒绝： w <- w x 0.94'),
    ('p', '翻译成人话：如果这对"破坏+修复"组合弄出了一个严格更好的方案，权重涨约 12% 再送 0.08（重赏）；方案只是被勉强接受（退火放行），涨约 4%（小赏）；方案被直接拒绝，权重打 94 折（轻罚）。赏罚都不重，为的是"慢慢学、别一两次走眼就把好算子打死"。权重始终被夹在 [0.2, 6.0] 之间，防止某个算子被彻底雪藏或一家独大。'),
    ('p', '手算一遍感受下速度。某算子初始权重 1.0：第一次立功变成 1.0 x 1.12 + 0.08 = 1.20；再立功变成 1.20 x 1.12 + 0.08 约 1.42。反过来，连续三次被拒：1.0 -> 0.94 -> 0.94 x 0.94 = 0.8836 -> 0.8836 x 0.94 约 0.8306。也就是说连吃三次闭门羹，权重大约缩到原来的 0.83 倍——有感觉，但远没被打死，配上 0.2 的下限，它随时有翻身机会。'),
    ('p', '选算子用的是**轮盘赌选择**（roulette wheel selection）：把每个算子的权重看成轮盘上的一块扇形，扇形越大指针停在那的概率越大。本项目的特别之处在于，扇形面积不只由全局权重决定（见 optimization/alns_core.py:ALNSCore._choose_operator）：'),
    ('formula', 'P(选中算子 i) 正比于 全局自适应权重(i) x 个体自带偏好(i)'),
    ('p', '翻译成人话：每个候选个体自己也揣着一份"我喜欢用哪些算子"的偏好表（destroy_weights / repair_weights），最终抽签概率是"全局长辈的经验"乘上"这个体自己的口味"。算个数：两个破坏算子全局权重 1.20 和 0.80，某个体对它们的偏好是 1.5 和 1.0，混合后面积是 1.20 x 1.5 = 1.80 和 0.80 x 1.0 = 0.80，合计 2.60，于是抽中概率约 69% 对 31%。**本项目实现差异**：这份个体偏好不是固定配置，而是基因的一部分，会跟着个体一起交叉、变异、被选择——"偏爱哪类算子"本身也在进化。'),
    ('p', '和教科书标准 ALNS（Ropke & Pisinger 2006）摆在一起对比，差异一眼可见：'),
    ('table', {
        'headers': ['对比点', '教科书 ALNS', '本项目实现'],
        'rows': [
            ['邻域作用对象', '直接改排程：从时刻表移除请求再插回', '改参数 op_bias，排程由仿真器重新生成'],
            ['修复方式', '贪婪插入 / regret-k，按插入成本逐个重插', '按图特征规则重写加分，无插入成本概念'],
            ['权重更新', '每跑完一段（segment）按得分和反应因子更新并重置', '逐次迭代乘性奖惩，无分段、无反应因子'],
            ['得分表', 'sigma1/sigma2/sigma3 三档加分', '无 sigma 表，压缩成三档乘性更新'],
            ['算子权重', '只有一套全局权重', '全局权重 x 个体偏好，个体偏好是基因'],
            ['接受准则', '单目标模拟退火比 f(x)', '先 Pareto 支配，互不支配再退到标量化退火'],
        ],
        'caption': '表5-1 教科书 ALNS 与本项目实现的对照',
    }),

    # ---------------------------------------------------------- 5.4
    ('h2', '5.4 接受准则：先讲道理，讲不清就掷骰子'),
    ('p', '新方案做出来，要不要用它替换当前方案？本项目是多目标优化（makespan、总延误等好几个目标同时在追），所以第一道判官是 **Pareto 支配**：如果新方案在所有目标上都不比旧方案差、且至少一个目标严格更好，就说新方案支配旧方案——这是"明显好"，二话不说直接换（见 optimization/alns_core.py:ALNSCore.refine）。反过来旧方案支配新方案，就是"明显差"，直接拒。'),
    ('p', '麻烦的是中间地带：新方案 makespan 更短但总延误更高，谁也不支配谁。这时退到第二道判官——**模拟退火**（simulated annealing）：把多目标先归一化压成一个标量和（scalar_score，alns_core.py:26），再按下面这条规则掷骰子：'),
    ('formula', 'delta = 新解标量和 - 旧解标量和； delta <= 0 必接受，否则以 exp(-delta / T) 的概率接受'),
    ('p', '翻译成人话：把多个目标折合成一个总分后，新方案总分更低（更好）就直接换；总分更高（更差）也不一棍子打死，而是掷一次骰子，差得越少、温度越高，放行的概率越大。这就是"明显好就换，差不多就掷骰子给机会"。给机会的意义在于：偶尔接受一个稍差的方案，搜索才有机会翻过局部最优的那道坎——这正是 5.1 说的"跳出去"在代码里的落脚点。'),
    ('p', '算两个数感受骰子的脾气。假设归一化后 delta = +0.5（新方案差一点），第 0 轮温度 T = 1.0：放行概率 exp(-0.5) 约 0.61，也就是六成机会被接受。跑到第 5 轮，T = 0.92^5 约 0.659，概率变成 exp(-0.5 / 0.659) 约 0.47，不足五成。如果新方案差得多，delta = +2.0，即使 T = 1.0 时概率也只有 exp(-2) 约 0.14——差得离谱的基本进不来。温度随轮次几何衰减：'),
    ('formula', 'T(k) = 1.0 x 0.92^k'),
    ('p', '翻译成人话：每跑一轮，温度打 92 折，骰子越来越"严格"——前期宽、后期紧。开头多给差方案机会（敢探索），快收尾时只认好方案（稳收敛）。注意本项目每个精英只精炼约 6 轮，温度从 1.0 降到 0.92^5 约 0.66，远没降到冰点，所以整个精炼更像"带一点退火扰动的爬坡"，而不是教科书里那种长长的退火曲线——迭代少，是本项目的有意取舍：全局探索交给 NSGA-III，ALNS 只做短平快的打磨。'),

    # ---------------------------------------------------------- 5.5
    ('h2', '5.5 图引导的破坏/修复算子：知道拆哪、补哪'),
    ('p', 'LNS 圈子里有句行话：破坏容易修复难，但比修复更难的是"拆哪"。随机拆不是不行，可车间里的工序不是一盘散沙——它们挂在一张关系网上。本项目用 **异构图** 把这张网建了出来：订单、任务、工序、机器、工装、人员是不同类型的节点，前后驱、配套、争抢资源是不同类型的边（见 knowledge/graph.py:HeterogeneousGraph）。图算好后，把每道工序的图特征预计算成 graph_features 注入优化器（见 hybrid_nsga3_alns.py:build_legacy_graph_features），常用的两个：'),
    ('bullets', [
        '**predecessor_depth**：一道工序前面叠了多少层前驱——深度越大，说明它越靠装配链上游，它一拖延，后面一串都跟着拖。',
        '**shared_resource_degree**：共享资源竞争度，量化"这道工序抢的工装/人员有多稀缺"：资源越稀缺、抢的人越多，度数越高。',
    ]),
    ('formula', 'shared_resource_degree = 1/可选机器数 + sum(1/工装实例数) + sum(1/技能人数) + 0.25 x (工装类型数 + 技能数)'),
    ('p', '翻译成人话：能干这活的机器越少、要用的工装和师傅越抢手，这个分数就越高——分数高的工序就是"资源冲突的火药桶"。有了这些图信息，破坏算子就能"指哪拆哪"，而不是蒙着眼睛乱拆。本项目一共配了 6 个破坏算子（见 hybrid_nsga3_alns.py:_destroy_ops）：'),
    ('table', {
        'headers': ['破坏算子', '拆谁', '依据'],
        'rows': [
            ['tardy_order_destroy', '超期订单的全部工序', '上一解的超期订单清单'],
            ['assembly_chain_destroy', '超期订单主任务沿前驱链的整条装配链', '任务前驱关系 DFS 回溯'],
            ['bottleneck_machine_destroy', '利用率 Top-2 机器上的工序', '上一解的机器利用率排名'],
            ['shared_tooling_destroy', '抢稀缺工装最凶的工序', 'shared_resource_degree 降序'],
            ['shared_personnel_destroy', '抢稀缺人员最凶的工序', 'shared_resource_degree 降序'],
            ['critical_predecessor_destroy', '深层关键前驱工序', 'predecessor_depth + 装配关键度'],
        ],
        'caption': '表5-2 六个图引导破坏算子一览',
    }),
    ('h3', 'assembly_chain_destroy：一拆拆整条链'),
    ('p', '这是最有"车间直觉"的一个算子（见 hybrid_nsga3_alns.py 中 _destroy_ops 的 assembly_chain_destroy）。逻辑：取出上一解里所有超期订单，从每个订单的**主任务**（通常是最后的装配任务）出发，沿 predecessor_task_ids 做 DFS 一路回溯，把链上每个任务的工序全收进来，再按 destroy_fraction 抽样拆除。'),
    ('p', '为什么要整条链一起拆？因为一条装配/配套链是荣辱与共的整体：主任务（比如总装）必须等所有配套件到齐才能开工，链条上任何一环迟到，主任务就跟着迟到。你只拆其中一道工序重排，链上其它工序原封不动，主任务的开工时刻基本不会变——改进被整条链"锁死"了。把整链的加分全拆掉重写，仿真器重跑时才可能让整条链整体提前。类比搬家：钢琴、琴凳、琴谱是一组，只重新规划其中一件的搬运顺序毫无意义，三件得一起重排。'),
    ('p', '在迷你案例里，每张订单只有一个任务（T1/T2/T3 无前驱任务），所以"链"退化成单个任务的工序：FIFO 解里超期的是 O1、O2，DFS 收进来的就是 {A1, A2, A3} 和 {B1, B2}，共 5 道，抽样 ceil(5 x 0.18) = 1 道。真实车间里一张订单往往拆成"零件任务 -> 部件任务 -> 总装任务"多级，那时 DFS 才会跨任务把几十道工序串成一条链，这个算子的威力才真正显现。'),
    ('h3', 'bottleneck_machine_destroy：疏导最堵的路口'),
    ('p', '**瓶颈机器**是全场最忙、大家排队最凶的那台设备——厨师里最慢的那位决定了出菜节奏。这个算子从上一解的分析数据里取出利用率排名前 2 的机器，把排在它们身上的工序收进来拆（见 _destroy_ops 的 bottleneck_machine_destroy；利用率排名由 optimization/objectives.py:build_schedule_analytics 预先算好）。道理很直白：全场节奏被瓶颈卡着，想改进总工期，就得把瓶颈机器上的活儿重新安排——分流一部分、或者调整先后顺序。类比交警治堵：与其满城乱跑，不如直奔最堵的那个路口疏导。'),
    ('p', '拿 FIFO 排程算一遍利用率：M1 干了 C1(2h)+B1(5h)+A2(2h)+A3(4h)=13h，利用率 13/13=1.00；M2 干了 A1(3h)+C2(6h)+B2(3h)=12h，利用率 12/13 约 0.92（平均 0.96，与实测一致）。Top-2 就是 M1、M2——迷你案例只有两台机器，等于全覆盖，看不出选择性；真实车间几十台机器时，"只动最忙的两台"的聚焦效果就非常值钱了。'),
    ('h3', 'shared_tooling_destroy / shared_personnel_destroy：拆最抢手的资源'),
    ('p', '两个孪生算子：把所有"需要工装（或需要人员技能）"的工序按 shared_resource_degree 降序排队，专拆抢得最凶的那批（见 _destroy_ops 的 shared_tooling_destroy、shared_personnel_destroy）。意图是把这些"火药桶"工序的优先级打乱重排，让它们在时间上错开，别再同一时刻抢同一套工装、同一位师傅。注意迷你案例无工装/人员约束，这两个算子会收进空集、无活可干——它们是为真实约束齐备的车间准备的。'),
    ('h3', '修复侧：补回去的五种思路'),
    ('p', '修复算子的统一套路是"全局特征权重微调 + 给被拆工序重写 op_bias"，差别只在"按什么规则加分"（见 hybrid_nsga3_alns.py:_repair_ops）：'),
    ('table', {
        'headers': ['修复算子', '一句话思路', 'op_bias 加分规则'],
        'rows': [
            ['due_date_repair', '交期越紧越先补', '按任务交期升序，前半名 +0.4-0.05x名次'],
            ['main_order_repair', '主订单、主任务优先', '+0.45 x 装配关键度'],
            ['bottleneck_smoothing_repair', '把活从瓶颈上摊平', '+0.3 x 瓶颈邻接度'],
            ['assembly_sync_repair', '配套件向主任务完工时刻对齐', '+0.35 x 归一化 order_main_gap'],
            ['shared_resource_repair', '把工序引向不挤的资源', '+0.22 x max(0, 1.2-稀缺度)'],
        ],
        'caption': '表5-3 五个修复算子一览',
    }),
    ('p', '重点说两个。assembly_sync_repair 解决"装配不同步"：分析数据里的 order_main_gap 记录每张订单"主任务完工时刻与其它任务完工时刻的差距"——gap 越大，说明配套件要么早到干等、要么迟到拖累装配。该算子把 gap 归一化后按 +0.35 x gap 给被拆工序加分：差距越大的订单，其工序加分越多，下次仿真就更早被排上，向主任务的完工时刻对齐。bottleneck_smoothing_repair 和 due_date_repair 分别是"给瓶颈周边工序按邻接度加分以摊平负荷"和"给交期紧的工序按名次加分"。注意 shared_resource_repair 的方向是反的：它给共享竞争**低**的工序加分（稀缺度越低、加分越多），意图是把工序往不挤的资源上分流，别误会成"越抢越补"。'),

    # ---------------------------------------------------------- 5.6
    ('h2', '5.6 与 NSGA-III 的混合：一个撒网，一个打磨'),
    ('p', 'ALNS 是局部搜索，天生只管"把眼前这个方案改好"，不管"别处是不是还有完全不一样的活法"。所以本项目把它嵌进 NSGA-III 里，组成 **memetic 算法**（文化基因算法，"进化 + 局部学习"的混合体）：NSGA-III 当外壳负责全局探索——24 个个体交叉变异、用参考点机制保住多样性，广撒网找一堆风格各异的好解；ALNS 当内嵌负责局部精炼——只对每代挑出的精英做短迭代打磨。分工一句话：NSGA-III 负责"找对人"，ALNS 负责"把人选培养好"。'),
    ('p', '还有两层工程细节值得一提。其一，不是所有个体都享受精炼：只有 NSGA-III 选择池里前 40% 的精英（elite_refine_ratio=0.4，至少 4 个）会被送进 ALNS，每个精英配一个全新的 ALNSCore，算子权重从 1.0 重新学起、随机种子各不相同（见 hybrid_nsga3_alns.py:_refine_solution）。其二，两阶段评估精度不同：进化阶段用近似评估快速筛，晋升和 ALNS 精炼阶段才用完整离散事件仿真——好钢用在刀刃上。整体流程如下：'),
    ('code', '+---------------------------------------------+\n| NSGA-III 全局进化（粗评估：近似仿真）       |\n| 24 个个体 x 12 代：交叉/变异/参考点选择     |\n+--------------------+------------------------+\n                     | 选精英 + 12% 随机个体\n                     v\n        精确仿真重评估（完整离散事件仿真）\n                     |\n                     v\n+---------------------------------------------+\n| ALNS 局部精炼（只打磨前 40% 精英，>=4 个）  |\n| 每个精英配一个新 ALNSCore，权重从 1.0 重学  |\n| 循环约 6 次：                               |\n|   选算子 -> 破坏 -> 修复 -> 完整仿真 -> 判定|\n+--------------------+------------------------+\n                     | 精炼结果汇入 Pareto 档案\n                     v\n        从档案中挑出多样的 12 个解输出'),
    ('p', '关键参数集中在 HybridConfig（hybrid_nsga3_alns.py:71），一行带过：population_size=24、generations=12、alns_iterations_per_candidate=6（每个精英精炼 6 轮）、elite_refine_ratio=0.4、target_solution_count=12。可以看到配置刻意"小步快跑"：种群不大、代数不多、精炼很短，因为每一次精确评估都要跑完整仿真，算力得花在最有希望的个体身上。'),
    ('p', '回顾全章：ALNS 在本项目里是一套"参数空间的自适应局部搜索"——拆的是 op_bias、补的是规则化加分、排程靠仿真器重跑；算子靠乘性奖惩和轮盘赌自适应地优胜劣汰；接受准则先 Pareto 支配再退火掷骰子；破坏和修复的依据来自工序依赖图与上一解的分析数据；而这一切只作用于 NSGA-III 筛出的精英。掌握了 ALNS 这把"精修刻刀"，下一章看混合优化器的整体实战表现时，你就明白每一分提升是从哪来的了。'),

    # ---------------------------------------------------------- 自测题
    ('h2', '自测题'),
    ('numbers', [
        'ALNS 里的字母 A（自适应）体现在本项目的哪两段代码里？它们各自做了什么？',
        'assembly_chain_destroy 为什么要把整条装配/配套链一起拆，而不是只拆链上最拖后的那一道工序？',
        '某破坏算子权重为 1.0，连续三次迭代做出来的方案都被拒绝，它的权重现在大约是多少？被轮盘赌选中的概率会如何变化？',
    ]),
    ('h3', '参考答案'),
    ('numbers', [
        '体现在两处。一是 optimization/alns_core.py:ALNSCore._update_operator 的乘性奖惩：方案改进权重 x1.12+0.08、仅接受 x1.04+0.03、被拒绝 x0.94，并夹在 [0.2, 6.0]；二是 ALNSCore._choose_operator 的轮盘赌选择：选中概率正比于全局权重乘个体自带偏好，且个体偏好本身是基因、随进化变化。合起来就是"谁好用就多用谁，越用越好用"。',
        '因为装配链是荣辱与共的整体：主任务（总装）必须等所有配套件到齐才能开工，链上任一环迟到主任务就跟着迟到。只拆一道工序，链上其它工序的优先级原封不动，仿真重跑后主任务开工时刻基本不变，改进被锁死；整链拆除重写加分，才可能让整条链整体提前、真正改善超期。',
        '每次被拒权重乘以 0.94：1.0 -> 0.94 -> 0.8836 -> 约 0.8306，即缩到原来的约 0.83 倍。轮盘赌中它的扇形面积同比缩小，被选中概率相应下降约 17%，但因为有 0.2 的下限保护且惩罚温和，它不会被永久雪藏，之后立功还能涨回来。',
    ]),
]
