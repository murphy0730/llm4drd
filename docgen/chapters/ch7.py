# -*- coding: utf-8 -*-
"""第7章 把一切串起来：端到端排产闭环"""

BLOCKS = [
    ("h1", "第7章 把一切串起来：端到端排产闭环"),

    ("p", "前六章我们分别学会了派工规则、离散事件仿真、帕累托前沿、进化算法和精确求解，本章把它们拼成一条完整流水线：数据怎么进来、问题怎么建模、三条求解路线怎么选、方案怎么上线、机器坏了怎么办。读完本章，你应该能徒手画出整个系统的全貌，并指出每一环对应项目里的哪个文件——这也是全书收尾的方式：用一张图、一个案例，把所有知识点串成闭环。"),

    ("h2", "7.1 一张图看清全流程"),

    ("p", "先用餐厅打个比方。把整个系统想成一家餐厅的一天：订单是客人点的菜，工序是做菜的步骤，机器是厨师，派工规则是排队叫号规则。前面几章分别研究了怎么叫号（第 1 章规则）、怎么在沙盘上演练一整天（第 2 章仿真）、怎么从一堆演练结果里挑出谁也不比谁差的几个（第 3 章帕累托前沿）。本章回答的问题是：从客人进门到打烊，完整的一天究竟怎么运转？答案在下面这张总流程图里，共六个环节。"),

    ("code",
     "[1] 数据: 订单 / 工序 / 机器  -->  ShopFloor\n"
     "        core/models.py:ShopFloor · data/generator.py 生成实例\n"
     "          |\n"
     "          v\n"
     "[2] 图建模: HeterogeneousGraph\n"
     "        knowledge/graph.py · 6 类节点 8 类边, 把车间结构化成网络\n"
     "          |\n"
     "          v\n"
     "[3] 求解三条路 (按场景三选一, 也可组合)\n"
     "   +--- A. 混合优化 NSGA-III + ALNS (中大规模, 多目标权衡)\n"
     "   |         optimization/hybrid_nsga3_alns.py\n"
     "   +--- B. 精确求解 CP-SAT (小规模, 要可证明的最优)\n"
     "   |         optimization/exact.py:ExactSolver\n"
     "   +--- C. LLM 规则进化 (让 AI 自动发明派工规则)\n"
     "             ai/evolution.py:EvolutionEngine"),

    ("code",
     "          |\n"
     "          v\n"
     "[4] 统一打分: 离散事件仿真 Simulator (全系统的计分器)\n"
     "        core/simulator.py · 三条路产出的方案用同一把尺子量 KPI\n"
     "          |\n"
     "          v\n"
     "[5] 选方案: 帕累托前沿 (互不碾压的解都留下, 按偏好挑)\n"
     "        optimization/pareto.py · optimization/archive.py\n"
     "          |\n"
     "          v\n"
     "[6] 上线执行: OnlineSchedulerV3 事件驱动推进\n"
     "        scheduling/online.py\n"
     "        +-- 机器故障 on_breakdown: 在制工序被抢占, 回就绪池重派\n"
     "        +-- 滚动窗口 reschedule: 剩余车间重仿真 x2, 新旧规则谁小用谁"),

    ("p", "逐个环节解释一下它们解决什么现实问题："),

    ("bullets", [
        "**数据层**（见 core/models.py:ShopFloor）：订单、任务、工序、机器这些对象组成一个 ShopFloor，就是“这个车间今天全部的家底”。`data/generator.py` 的 generate 按参数随机造出这样的车间实例，让实验不用等真实订单。",
        "**图建模**（见 knowledge/graph.py:HeterogeneousGraph）：把车间的对象与关系转成一张有向异构图——6 类节点（订单/任务/工序/机器/工装/人员）、8 类边（如 task_has_operation、machine_eligible）。原本散落在各对象里的依赖关系从此变成一张可以遍历的网络，混合优化器正是靠它提取结构特征。",
        "**三条求解路线**：路 A 混合优化（optimization/hybrid_nsga3_alns.py，NSGA-III 做全局进化、ALNS 做局部破坏重建）；路 B 精确求解（optimization/exact.py:ExactSolver，基于 OR-Tools CP-SAT，小规模能给出数学证明的最优）；路 C LLM 规则进化（ai/evolution.py，让大模型自动写规则，7.3 节专讲）。",
        "**统一打分**（见 core/simulator.py:Simulator）：无论哪条路产出的方案，最终都交给同一个离散事件仿真器跑一遍，输出 makespan、总延误、利用率等同一套 KPI——同一把尺子量所有选手，比分才公平。",
        "**帕累托前沿选方案**（见 optimization/pareto.py:ParetoOptimizer、optimization/archive.py:ParetoArchive）：多目标下通常没有谁全面碾压谁，把互不支配的解都留在前沿上，由人按当下偏好挑一个。",
        "**上线执行**（见 scheduling/online.py:OnlineSchedulerV3）：选定规则后进入事件驱动的在线调度——时钟一个事件一个事件往前跳（advance）；机器故障 on_breakdown 触发抢占式重排；滚动窗口 reschedule 把剩余车间切出来，对新旧规则各做一次完整仿真，谁的总延误小就切换到谁。",
    ]),

    ("p", "有一处需要如实说明：OnlineSchedulerV3 没有专门的插单接口，动态新订单的处理路径是外部更新车间数据后重建调度器，或走 reschedule 的剩余车间重仿真。另外，第 2 环节那张图主要服务混合优化器与前端可视化；规则仿真和在线调度并不依赖它——每条路只取自己需要的零件。"),

    ("quote", "**端到端闭环**：数据进来，方案出去；执行中一有扰动就重仿真、重决策。仿真器是整个闭环的心脏，各求解器只是围绕它的不同“提案者”。"),

    ("h2", "7.2 四步骤回顾：从实例到结果"),

    ("p", "把上面六个环节压缩成日常操作的四个步骤，就是本书一直在走的路线：生成实例 → 建模 → 求解 → 看结果。下面逐步回顾，并把三种求解武器的选择问题一次说清。"),

    ("h3", "第 1 步 生成实例"),

    ("p", "data/generator.py 的 generate 按“几台机器、几张订单、工序数、交期松紧”这类参数随机造一个 ShopFloor；generate_training_set 一次造一批（默认 5 个），专门给进化算法当训练集。本书的迷你案例（3 张订单、7 道工序、2 台机器）就是一个手工固定的实例，目的是让每个数字都能亲手验算。"),

    ("h3", "第 2 步 建模"),

    ("p", "实例本身就是 ShopFloor 对象；调用其内部目标推导（见 core/models.py:derive_internal_targets）会沿工艺链把订单交期反推出每道工序的**派生交期**——订单的截止时刻扣除后续各工序工时，逐段倒排到本工序。迷你案例里 A1=5、A2=7、A3=11、B1=5、B2=8、C1=8、C2=14 就是这么来的。随后图建模环节再把它转成依赖网络，供混合优化器读取结构。"),

    ("h3", "第 3 步 求解：三种武器怎么选"),

    ("p", "三条路线不是竞争关系，而是各有适用场景的三件武器。选哪件，取决于你要多快、问题多大、要不要最优证明："),

    ("bullets", [
        "**要快**：只要一个像样的排程且立刻就要，直接用 11 条内置规则之一跑仿真（见 core/rules.py 的 BUILTIN_RULES）；要同时权衡多个目标、又是中大规模，跑 NSGA-III+ALNS 混合优化，它给你一整条前沿而不是单一答案。",
        "**要最优证明**：规模小（几十道工序以内）、且必须盖棺定论时，用 CP-SAT 精确求解（见 optimization/exact.py:ExactSolver）。迷你案例里它以 makespan 为目标证得最优 13，以总延误为目标证得最优 0，均返回 OPTIMAL。",
        "**要量身定制**：内置规则都不顺手、或想让 AI 针对自家车间特点自动发明规则时，用 LLM 规则进化（见 ai/evolution.py），它是第 1 章 compile_rule_from_code 的全自动版本。",
    ]),

    ("table", {
        "headers": ["场景", "推荐武器", "对应模块", "理由"],
        "rows": [
            ["只要一个像样的排程，立刻要", "内置规则 + 仿真", "core/rules.py + core/simulator.py", "11 条内置规则即取即用，一次仿真秒出 KPI"],
            ["中大规模、多目标权衡", "NSGA-III + ALNS 混合优化", "optimization/hybrid_nsga3_alns.py", "进化保多样性、ALNS 局部精修，输出整条帕累托前沿"],
            ["小规模、要可证明的最优", "CP-SAT 精确求解", "optimization/exact.py", "迷你案例 7 道工序两个目标都拿到 OPTIMAL 证明"],
            ["想自动发明规则 / 内置规则不合身", "LLM 规则进化", "ai/evolution.py", "LLM 在规则代码空间里做进化搜索，适应当前车间的数据分布"],
            ["上线执行、处理故障与变更", "在线调度闭环", "scheduling/online.py", "事件驱动推进，故障抢占 + 滚动窗口重排"],
        ],
        "caption": "表 7-1 什么时候用哪招：三种求解武器与执行闭环的选择指南",
    }),

    ("p", "**本项目实现差异**：经典滚动时域优化每期都重新求解一个数学规划模型；本项目的 reschedule 更轻量——不解新模型，而是把剩余车间快照交给 Simulator，对新旧两条规则各做一次完整仿真，直接比较总延误后决定是否切换规则（见 scheduling/online.py:reschedule）。它回答的不是“最优排程是什么”，而是更实际的“现在换条规则值不值”。"),

    ("h3", "第 4 步 看结果：甘特图 + KPI"),

    ("p", "仿真结果里有两样东西：甘特图数据（每道工序在哪台机器、几点开工几点完工）和 KPI（makespan、总延误、延误任务数、平均流程时间、利用率）。回顾迷你案例：EDD、MST、CR 三条规则都拿到总延误 0、makespan 14 的成绩；CP-SAT 进一步证明 0 延误就是数学最优——经验规则与精确解互相印证，这正是“计分器统一”的红利：不同来源的答案可以直接摆在一起比。"),

    ("h2", "7.3 专题：LLM 规则进化——让 AI 替你写叫号规则"),

    ("p", "第 1 章介绍过 compile_rule_from_code（见 core/rules.py:compile_rule_from_code）：把一段 Python 源码 exec 进只含 math 的命名空间，返回第一个非下划线开头的可调用对象。当时它是“你手写规则”的入口；本节你会发现它同时是“AI 写规则”的入口——ai/evolution.py 围绕它做了一整套进化算法。"),

    ("quote", "**核心思想一句话**：把派工规则本身当作生物个体——它的基因就是一段 Python 规则源码——让 LLM 扮演繁育者，让仿真器扮演大自然；优胜劣汰若干代之后，留下最适应你车间的那条规则。"),

    ("h3", "个体与基因：RuleIndividual"),

    ("p", "ai/evolution.py:RuleIndividual 的字段里，code 就是基因：一段完整的 def evolved_rule(op, machine, f, shop) 源码字符串；fitness 是适应度（总延误，越小越好）；llm_score 与 hybrid_score 用于综合排名。它的 compiled 属性只有一行：compile_rule_from_code(self.code)——基因瞬间变成可执行的规则函数，和第 1 章你手工传源码走的是同一座桥。"),

    ("h3", "繁育者：LLMInterface 双专家"),

    ("p", "ai/evolution.py:LLMInterface 封装两类角色：**LLM-A**（算法专家）负责生成、交叉、变异规则代码；**LLM-S**（调度专家）负责给优秀个体打分点评。有 API key 就走真实的 OpenAI 兼容接口；没有 key（或调用异常）就静默回退到模板拼装——从 9 个特征原子里随机抽 4 个、配随机权重与公式模板，拼出一段合法规则源码。换句话说，不联网、不配 key，这台进化引擎照样能跑，只是“创造力”打折。"),

    ("h3", "大自然：仿真器又一次当计分器"),

    ("p", "每个个体的适应度这样算：先用 compile_rule_from_code 编译它的 code，然后在每个训练实例上各跑一次完整仿真，取平均总延误作为 fitness（见 ai/evolution.py:EvolutionEngine.evolve 的评估循环）。编译或仿真出任何异常，适应度直接记为无穷大——相当于天生残疾、直接淘汰。注意，这是仿真器在本章闭环里的又一次登场：给规则直接打分、给混合优化当评估器之后，这回它给进化中的每条规则当生存裁判。"),

    ("h3", "配置与一代的循环"),

    ("p", "ai/evolution.py:EvolutionConfig 的默认参数不大：population_size=8（每代 8 条规则）、max_generations=15（最多 15 代）、elite_size=3（每代 3 条精英直接保送下一代）、patience=5（连续 5 代无改进就早停）。一代的流程如下："),

    ("numbers", [
        "评估：种群 8 条规则逐一编译，在每个训练实例上各仿真一次，取平均总延误作为 fitness。",
        "专家点评：按 fitness 排序后，LLM-S 给前 3 名打 0~10 分并写出结构化改进建议。",
        "综合排名：把归一化 fitness 与 LLM 评分合成 hybrid_score（越小越好），重排种群。",
        "优胜劣汰：前 3 名精英原样保留；其余位置以约 50% 概率做交叉（提示词附上两个精英的源码与 fitness，让 LLM-A 组合出更好的规则）或变异（让 LLM-A 改进某条精英规则），生成下一代。",
        "早停判断：若最佳 fitness 连续 patience 代没有改善，提前收工，返回历史最佳个体。",
    ]),

    ("formula", "hybrid_score = 0.7 × 归一化fitness + 0.3 × (10 − llm_score) / 10"),

    ("p", "翻译成人话：一条规则的最终名次，七分看它真刀真枪仿真出来的延误水平，三分看 LLM-S 专家对它“代码气质”的评分——防止某些规则只会应付训练实例、结构上却是歪门邪道。0.7 与 0.3 正是 EvolutionConfig 里的 objective_weight 与 llm_eval_weight。"),

    ("p", "手算一个例子（数字全部取自迷你案例的真实仿真结果）。输入：某代种群中最佳 fitness=0（EDD 的水平）、最差 fitness=6（FIFO 的水平），规则 X 的 fitness=3（SPT 的水平），LLM-S 给 X 打 8 分。逐步计算：归一化 fitness = (3 − 0) / (6 − 0) = 0.5；LLM 分项 = (10 − 8) / 10 = 0.2；hybrid_score = 0.7 × 0.5 + 0.3 × 0.2 = 0.35 + 0.06 = 0.41。结论：X 的综合分为 0.41，它能不能进精英，取决于同代其他个体的分数是否比它更小。"),

    ("p", "把整个循环串成一句口诀：**LLM 写规则 → 编译成函数 → 仿真打分 → 优胜劣汰 → 让 LLM 参考高分规则写更好的**。种群也不是冷启动：精英种子来自 BUILTIN_RULES（默认 ATC、EDD、KIT_AWARE 三条内置规则转成独立可编译源码），其余个体由 LLM-A 现场创作。"),

    ("p", "**本项目实现差异**：经典遗传编程在语法树上做随机交叉与变异，产物常常又臭又长、难以卒读；本项目把交叉变异外包给 LLM——更像“请一位读过前辈代码的工程师重写”，而非随机拼接代码片段。代价是结果质量依赖 LLM 的水平，无 API key 时模板回退能给出的多样性有限。"),

    ("h2", "7.4 迷你案例总复习：一个案例走完七章"),

    ("p", "还是那个 3 订单、7 工序、2 机器的迷你案例：订单 A 齿轮轴交期 11h（A1 粗车 3h → A2 精车 2h → A3 磨削 4h）、B 法兰盘交期 8h（B1 铣面 5h → B2 钻孔 3h）、C 支架交期 14h（C1 下料 2h → C2 折弯 6h），t=0 全部放行。先看 EDD 规则跑出来的完整排程，它也是本案例的满分答卷之一："),

    ("code",
     "EDD 规则排程甘特图 (makespan=14, 总延误=0, 每格 1 小时)\n"
     "   0       2       4       6       8       10      12      14\n"
     "M1 |A1 |A1 |A1 |A2 |A2 |B2 |B2 |B2 |C2 |C2 |C2 |C2 |C2 |C2 |\n"
     "M2 |B1 |B1 |B1 |B1 |B1 |C1 |C1 |A3 |A3 |A3 |A3 |   |   |   |"),

    ("p", "对照交期验一下：A3 于时刻 11 完工，正好踩中订单 A 的交期 11；B2 于时刻 8 完工，踩中 B 的交期 8；C2 于时刻 14 完工，踩中 C 的交期 14——三张订单全部准时，总延误 0。下面用这个案例把全书七章各回响一句："),

    ("table", {
        "headers": ["章", "主题", "在迷你案例上的一句话回响"],
        "rows": [
            ["第0章", "案例与指标", "makespan 是“最后一桌客人吃完的时刻”：FIFO 下为 13，即 A3 在 M1 上 9→13 完工"],
            ["第1章", "规则打分", "t=0 时 MST 给 A1 打 4 分、B1 打 3 分、C1 打 0 分，分数最大者先上机器，故选 A1"],
            ["第2章", "仿真计分", "同一条 SPT 规则交给仿真器，跑出 makespan=15、总延误=3、利用率 0.83"],
            ["第3章", "帕累托前沿", "makespan 最优 13 与总延误最优 0 不可兼得（CP-SAT 分别证实），两个解互不支配、同留前沿"],
            ["第4章", "进化种群", "8 条规则个体各自跑仿真拿 fitness，平均总延误最小者当精英"],
            ["第5章", "打磨精英", "ALNS 式思路：把精英方案局部破坏再重建，好比打烊前微调明天的排班"],
            ["第6章", "证明最优", "CP-SAT 对总延误目标返回 OPTIMAL=0：数学上证明零延误可行，EDD 的经验被盖章"],
            ["第7章", "端到端闭环", "以上全部发生在同一条流水线上，仿真器从头到尾反复扮演计分器"],
        ],
        "caption": "表 7-2 迷你案例上的七章回响",
    }),

    ("h2", "7.5 下一步：亲手跑一遍的学习路线"),

    ("p", "书读到这里，最好的下一步是动手。下面七步由浅入深，每一步都对应仓库里真实存在的文件，按顺序走完，你就从读者变成了能改造这套系统的人："),

    ("numbers", [
        "先跑通 demo.py：它把“生成实例 → 构图 → 仿真”整条链路走一遍，看到甘特图和 KPI 输出，你就有了手感。",
        "用 compile_rule_from_code 写一条自己的规则（比如“slack 为负时优先级翻倍”），丢给 Simulator 跑仿真，和内置 EDD 的 KPI 正面对比。",
        "读 core/simulator.py 的主循环 Simulator.run：看懂释放检查、派工、完工结算三段如何交替推进时钟，你就懂了全项目的计分器。",
        "调 HybridConfig 的参数（种群规模、ALNS 迭代数等，见 optimization/hybrid_nsga3_alns.py:HybridConfig），重跑混合优化，观察帕累托前沿怎么变密、怎么移动。",
        "读 optimization/exact.py:ExactSolver 的建模：看工序区间变量、先后约束、机器不重叠约束怎样写成 CP-SAT 的语言。",
        "尝试 ai/evolution.py 的 LLM 进化：不配 API key 也能跑模板回退；配上 key 后观察 LLM-A 写的规则一代代变强，留意每代最优 fitness 的下降曲线。",
        "最后读 scheduling/online.py 的在线闭环：重点看 advance 的事件跳跃、on_breakdown 的抢占重排、reschedule 的双仿真比较——那是本书知识真正上线的地方。",
    ]),

    ("h2", "自测题"),

    ("numbers", [
        "在端到端闭环里，core/simulator.py 的仿真器一共以哪几种角色出现？为什么说“一代 8 条规则、5 个训练实例”意味着 LLM 进化每代要跑 40 次完整仿真？",
        "客户丢给你一个 500 道工序的排产问题，要求“尽快给出多个不错的方案供挑选”。你该选 CP-SAT 精确求解还是 NSGA-III+ALNS 混合优化？为什么？",
        "LLM 规则进化中，一个个体的适应度 fitness 具体是怎么算出来的？hybrid_score 又在适应度之外补了什么？",
    ]),

    ("h3", "参考答案"),

    ("numbers", [
        "四种角色：给单条规则直接打分；给混合优化（NSGA-III+ALNS）的候选方案当评估器（optimization/pareto.py 与 optimization/hybrid_nsga3_alns.py 均复用 Simulator）；给 LLM 进化当适应度函数；给在线 reschedule 当比较器——而且 reschedule 一次要调两遍，旧规则一遍、新规则一遍（见 scheduling/online.py:reschedule）。因为适应度定义为“该规则在所有训练实例上仿真的平均总延误”，每条规则在每个实例上都要各跑一次，所以 8 条规则 × 5 个训练实例 = 每代 40 次完整仿真（见 ai/evolution.py:EvolutionEngine.evolve）。",
        "选混合优化。CP-SAT 的优势是给出“数学证明的最优”，但证明代价随规模爆炸式增长，500 道工序往往等不到 OPTIMAL；混合优化不承诺最优，却能在可接受时间内输出一整条帕累托前沿，正好满足“多个不错的方案供挑选”。CP-SAT 更适合迷你案例这种几十道工序以内、需要盖棺定论的场合——本项目里它证明了总延误 0 可行、makespan 13 最优。",
        "把个体的 code 用 compile_rule_from_code 编译成规则函数，在每个训练实例上各跑一次完整仿真，取平均总延误作为 fitness；编译或仿真异常则记为无穷大，直接淘汰（见 ai/evolution.py:EvolutionEngine.evolve）。hybrid_score = 0.7 × 归一化 fitness + 0.3 × (10 − llm_score) / 10，在仿真成绩之外补了 LLM-S 专家对规则代码质量的评分，防止只会刷训练集、结构却不合理的规则胜出。",
    ]),
]
